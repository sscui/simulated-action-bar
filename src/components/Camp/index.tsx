import React, { useState } from 'react';
import {IShikigami} from "../interface";

export const Camp: React.FC<{}> = () => {
  const [shikigamis, setShikigamis] = useState<IShikigami[]>([]);

  return <div>  
    test </div>
} 